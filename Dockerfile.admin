FROM node:22-alpine
WORKDIR /app
ENV NODE_ENV=production
ENV PORT=8080

COPY dist-admin/package.json ./
RUN npm install --omit=dev --no-audit --no-fund

COPY dist-admin/index.cjs ./

EXPOSE 8080
CMD ["node", "index.cjs"]
